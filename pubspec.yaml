name: command_bar
description: Flutter implementation of the Command Bar (or Pallette). Can be brought up via a keyboard shortcut.
version: 0.0.1
homepage: https://github.com/TNorbury/command_bar
issue_tracker: https://github.com/TNorbury/command_bar/issues
repository: https://github.com/TNorbury/command_bar

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  substring_highlight: ^1.0.33

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

